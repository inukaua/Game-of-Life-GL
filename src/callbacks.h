#ifndef CALLBACKS_H
#define CALLBACKS_H

#include <glad/glad.h>
#include <GLFW/glfw3.h>

// Resizes the OpenGL viewport to fit the window with glViewPort
void framebufferSizeCallback(GLFWwindow* window, int width, int height );
#endif
